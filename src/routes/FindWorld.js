import Finding from "../components/Finding";
import styles from "./FindWorld.module.css";

function FindWorld() {
  return (
    <div className={styles.container}>
      <Finding />
    </div>
  );
}

export default FindWorld;
